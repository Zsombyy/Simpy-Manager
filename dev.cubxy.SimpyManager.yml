app-id: dev.cubxy.SimpManager
runtime: org.freedesktop.Platform
runtime-version: "23.08"
sdk: org.freedesktop.Sdk
command: simpymanager
finish-args:
  - --share=network
  - --filesystem=home

modules:
  - name: simpymanager
    buildsystem: cargo
    sources:
      - type: dir
        path: .